==============
:class:`Model`
==============
.. module:: p4.model

The Model class has very little user interface.  The user mostly
sets model attributes via Tree methods.

The `Model.relRatesAreFree` Boolean says whether relRates are free
parameters.

.. autoclass:: Model
   :members: dump, getBigQ


